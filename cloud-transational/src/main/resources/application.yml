server:
  port: 8080

spring:
  datasource:
    username: root
    password: XinTong2019
    url: jdbc:mysql://localhost:3306/tran?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapping.xml
  type-aliases-package: com.xintong.cloudtransational.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug
